# S3.1 verification (temporary): structural smoke of the generic trusted builder.
# Calls container-build-attest.yml (pinned SHA) with NO inputs and NO secrets;
# build config comes from .github/container-build.json in this source revision.
# No dispatch, no promotion.
name: s3-builder-smoke

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

jobs:
  call:
    uses: toreau/gh-workflows/.github/workflows/container-build-attest.yml@373df7517487bd20ac55a0986926677c0ddcbcf6
